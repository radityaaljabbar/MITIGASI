NODE_ENV=production
PORT=8080

# Database (will be injected by Cloud Run)
DB_HOST=${DB_HOST}
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}
DB_NAME=${DB_NAME}

# JWT Secret
JWT_SECRET=${JWT_SECRET}
JWT_EXPIRE=3d
COOKIE_EXPIRE=3

# CORS
CORS_ORIGIN=${CORS_ORIGIN}